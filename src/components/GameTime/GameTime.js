// var secondLoop = setInterval(incrementOneSecond, 500);
import React from 'react';
import PropTypes from 'prop-types';

const GameTime = () => { setInterval(DoATick, 1000) };

GameTime.propTypes = {
};

export default GameTime;
